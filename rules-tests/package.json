{
  "name": "rules-tests",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "firebase emulators:exec --only firestore --project trackbyphoto-test 'vitest run'",
    "test:watch": "firebase emulators:exec --only firestore --project trackbyphoto-test 'vitest'"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^4.0.1",
    "firebase": "^11.0.2",
    "typescript": "^5.7.2",
    "vitest": "^3.0.5"
  }
}
